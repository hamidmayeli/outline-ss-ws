# API Host Configuration
# Use /api for production (proxied through nginx)
# Use http://localhost:5000 for local backend development
# Use http://localhost:3001/api for the mock API server
VITE_API_HOST=/api
